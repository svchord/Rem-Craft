export const config = {
    app: {
        calendar: true, //日历按钮
        comment: true, //行内评论
    },
    plugin: {
        tabBar: true, // 顶部页签栏
        status: true, // 底部状态栏
        listIndent: true, // 文档树缩进
        bullet: true, //子弹流列表块辅助线
    },
};
